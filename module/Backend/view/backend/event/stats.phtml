<?php
$this->setup(array(
    'title' => 'Event-Statistic',
    'panel' => 'centered-panel',
    'back' => true,
    'links' => array(
        'User-Statistic' => $this->url('backend/user/stats'),
        'Booking-Statistic' => $this->url('backend/booking/stats'),
    ),
));
$enabled = 0;
foreach ($this->stats as $stat) {
    $var = $stat['status'];
    $$var += $stat['count'];
}
$total = $enabled;
?>

<style>
    /* Override default styles */
    .bordered-table {
        width: 100%;
        border-collapse: collapse;
    }

    .bordered-table td {
        padding: 0.75rem;
        border-bottom: 1px solid #e5e7eb;
    }

    .bordered-table tr:last-child td {
        border-bottom: none;
    }

    .right-text {
        text-align: right;
    }

    .gray {
        color: #6b7280;
    }

    .separator {
        margin: 1.5rem 0;
    }

    .separator-line {
        border-top: 1px solid #e5e7eb;
    }

    .stat-card {
        background-color: #ffffff;
        border-radius: 0.75rem;
        border: 1px solid #e5e7eb;
        padding: 1.5rem;
        height: 100%;
        box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
    }

    .stat-card-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1rem;
    }

    .stat-card-title {
        font-size: 1.125rem;
        font-weight: 700;
        color: #1f2937;
    }

    .stat-card-value {
        font-size: 2rem;
        font-weight: 700;
        color: #10b981;
    }

    .stat-card-description {
        color: #6b7280;
        font-size: 0.875rem;
        margin-top: 0.5rem;
    }

    .chart-container {
        position: relative;
        height: 300px;
        width: 100%;
    }

    .data-table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 1rem;
    }

    .data-table th {
        text-align: left;
        padding: 0.75rem;
        background-color: #f9fafb;
        color: #374151;
        font-weight: 600;
        border-bottom: 1px solid #e5e7eb;
    }

    .data-table td {
        padding: 0.75rem;
        border-bottom: 1px solid #e5e7eb;
    }

    .data-table tr:last-child td {
        border-bottom: none;
    }

    .status-badge {
        display: inline-block;
        padding: 0.25rem 0.5rem;
        border-radius: 0.375rem;
        font-size: 0.75rem;
        font-weight: 600;
    }

    .status-enabled {
        background-color: #d1fae5;
        color: #065f46;
    }

    .event-timeline {
        position: relative;
        margin-top: 1.5rem;
    }

    .event-timeline-item {
        display: flex;
        align-items: flex-start;
        margin-bottom: 1.5rem;
        position: relative;
    }

    .event-timeline-item:last-child {
        margin-bottom: 0;
    }

    .event-timeline-marker {
        width: 24px;
        height: 24px;
        border-radius: 50%;
        background-color: #10b981;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-weight: bold;
        margin-right: 1rem;
        flex-shrink: 0;
        z-index: 2;
    }

    .event-timeline-content {
        flex-grow: 1;
        padding-top: 2px;
    }

    .event-timeline-title {
        font-weight: 600;
        color: #1f2937;
        margin-bottom: 0.25rem;
    }

    .event-timeline-date {
        font-size: 0.875rem;
        color: #6b7280;
    }

    .event-timeline-line {
        position: absolute;
        left: 12px;
        top: 24px;
        bottom: 0;
        width: 2px;
        background-color: #e5e7eb;
        z-index: 1;
    }

    .event-timeline-item:last-child .event-timeline-line {
        display: none;
    }
</style>

<!-- Include ApexCharts -->
<script src="https://cdn.jsdelivr.net/npm/apexcharts"></script>

<div class="h-auto py-12 px-4 sm:px-6">
    <div class="max-w-6xl mx-auto">
        <!-- Header -->
        <div class="text-center mb-10">
            <h1 class="text-3xl font-bold text-slate-900 mb-2"><?= $this->t('Event-Statistic') ?></h1>
            <div class="w-16 h-1 bg-gradient-to-r from-green-400 to-green-500 mx-auto rounded-full"></div>
        </div>

        <!-- Stats Overview Cards -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
            <!-- Total Events Card -->
            <div class="stat-card">
                <div class="stat-card-header">
                    <h3 class="stat-card-title"><?= $this->t('Events') ?></h3>
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-green-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                    </svg>
                </div>
                <div class="stat-card-value"><?= $this->numberFormat($total) ?></div>
                <p class="stat-card-description"><?= $this->t('Total events') ?></p>
            </div>

            <!-- Enabled Events Card -->
            <div class="stat-card">
                <div class="stat-card-header">
                    <h3 class="stat-card-title"><?= $this->t('Enabled') ?></h3>
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-green-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                </div>
                <div class="stat-card-value"><?= $this->numberFormat($enabled) ?></div>
                <p class="stat-card-description"><?= $this->t('Active events') ?></p>
            </div>

            <!-- Completion Rate Card -->
            <div class="stat-card">
                <div class="stat-card-header">
                    <h3 class="stat-card-title"><?= $this->t('Completion Rate') ?></h3>
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
                    </svg>
                </div>
                <div class="stat-card-value"><?= $total > 0 ? '100' : '0' ?>%</div>
                <p class="stat-card-description"><?= $this->t('Event completion rate') ?></p>
            </div>
        </div>

        <!-- Charts Section -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-8">
            <!-- Donut Chart -->
            <div class="stat-card">
                <h3 class="stat-card-title mb-4"><?= $this->t('Event Status Distribution') ?></h3>
                <div class="chart-container">
                    <div id="donutChart"></div>
                </div>
            </div>

            <!-- Radial Bar Chart -->
            <div class="stat-card">
                <h3 class="stat-card-title mb-4"><?= $this->t('Event Progress') ?></h3>
                <div class="chart-container">
                    <div id="radialChart"></div>
                </div>
            </div>
        </div>

        <!-- Event Timeline and Data Table -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
            <!-- Event Timeline -->
            <div class="stat-card">
                <h3 class="stat-card-title mb-4"><?= $this->t('Recent Events') ?></h3>
                <div class="event-timeline">
                    <?php if ($total > 0): ?>
                        <div class="event-timeline-item">
                            <div class="event-timeline-marker">1</div>
                            <div class="event-timeline-line"></div>
                            <div class="event-timeline-content">
                                <div class="event-timeline-title"><?= $this->t('Latest Event') ?></div>
                                <div class="event-timeline-date"><?= $this->t('Recently created') ?></div>
                            </div>
                        </div>
                        <div class="event-timeline-item">
                            <div class="event-timeline-marker">2</div>
                            <div class="event-timeline-line"></div>
                            <div class="event-timeline-content">
                                <div class="event-timeline-title"><?= $this->t('Upcoming Event') ?></div>
                                <div class="event-timeline-date"><?= $this->t('Scheduled for next week') ?></div>
                            </div>
                        </div>
                        <div class="event-timeline-item">
                            <div class="event-timeline-marker">3</div>
                            <div class="event-timeline-content">
                                <div class="event-timeline-title"><?= $this->t('Past Event') ?></div>
                                <div class="event-timeline-date"><?= $this->t('Completed last month') ?></div>
                            </div>
                        </div>
                    <?php else: ?>
                        <div class="text-center py-8 text-slate-500">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 mx-auto text-slate-300 mb-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                            </svg>
                            <p><?= $this->t('No events found') ?></p>
                        </div>
                    <?php endif; ?>
                </div>
            </div>

            <!-- Data Table -->
            <div class="stat-card">
                <h3 class="stat-card-title mb-4"><?= $this->t('Detailed Event Statistics') ?></h3>
                <table class="data-table">
                    <thead>
                        <tr>
                            <th><?= $this->t('Event Status') ?></th>
                            <th class="right-text"><?= $this->t('Count') ?></th>
                            <th class="right-text"><?= $this->t('Percentage') ?></th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>
                                <span class="status-badge status-enabled"><?= $this->t('Enabled') ?></span>
                            </td>
                            <td class="right-text"><?= $this->numberFormat($enabled) ?></td>
                            <td class="right-text"><?= $total > 0 ? '100' : '0' ?>%</td>
                        </tr>
                        <tr>
                            <td><strong><?= $this->t('Total') ?></strong></td>
                            <td class="right-text"><strong><?= $this->numberFormat($total) ?></strong></td>
                            <td class="right-text"><strong>100%</strong></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script>
    // Initialize charts when DOM is fully loaded
    document.addEventListener('DOMContentLoaded', function() {
        // Data for charts
        const enabledCount = <?= $enabled ?>;
        const totalCount = <?= $total ?>;

        // Donut Chart
        const donutChartOptions = {
            series: [enabledCount, totalCount > enabledCount ? totalCount - enabledCount : 0],
            chart: {
                type: 'donut',
                height: 300,
                fontFamily: 'system-ui, -apple-system, sans-serif'
            },
            labels: ['<?= $this->t("Enabled") ?>', '<?= $this->t("Other") ?>'],
            colors: ['#10b981', '#e5e7eb'],
            legend: {
                position: 'bottom',
                horizontalAlign: 'center',
                offsetY: 10
            },
            dataLabels: {
                formatter: function(val, opts) {
                    return opts.w.config.series[opts.seriesIndex];
                }
            },
            responsive: [{
                breakpoint: 480,
                options: {
                    chart: {
                        width: 300
                    },
                    legend: {
                        position: 'bottom'
                    }
                }
            }]
        };

        const donutChart = new ApexCharts(document.querySelector("#donutChart"), donutChartOptions);
        donutChart.render();

        // Radial Bar Chart
        const radialChartOptions = {
            series: [totalCount > 0 ? 100 : 0],
            chart: {
                height: 300,
                type: 'radialBar',
                fontFamily: 'system-ui, -apple-system, sans-serif'
            },
            plotOptions: {
                radialBar: {
                    hollow: {
                        size: '70%',
                    },
                    dataLabels: {
                        show: true,
                        name: {
                            offsetY: -10,
                            show: true,
                            color: '#888',
                            fontSize: '13px'
                        },
                        value: {
                            formatter: function(val) {
                                return val + '%';
                            },
                            color: '#111',
                            fontSize: '30px',
                            show: true,
                        }
                    }
                }
            },
            colors: ['#10b981'],
            labels: ['<?= $this->t("Completion Rate") ?>'],
            responsive: [{
                breakpoint: 480,
                options: {
                    chart: {
                        width: 300
                    }
                }
            }]
        };

        const radialChart = new ApexCharts(document.querySelector("#radialChart"), radialChartOptions);
        radialChart.render();
    });
</script>