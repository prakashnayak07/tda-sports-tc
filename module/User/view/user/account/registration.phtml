<?php
$this->setup(array(
    'title' => 'Registration',
    'panel' => 'centered-panel',
    'messages' => array(
        'error' => sprintf($this->loginMessage ?: '', $this->loginDetent ? $this->timeFormat($this->loginDetent) : ''),
    ),
    'back' => false,
    'links' => array(
        'Already have an account?' => $this->url('user/login'),
    ),
));
?>
<!DOCTYPE html>
<html lang="<?= $this->config('i18n.locale') ?>">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= $this->option(['client.name.short', 'service.name.full']) ?> - Registration</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        brand: '#93b353',
                        'brand-dark': '#b0c817',
                    },
                    fontFamily: {
                        sans: ['Inter', 'system-ui', 'sans-serif'],
                    },
                }
            }
        }
    </script>
</head>

<body class="bg-gray-50 font-sans antialiased">
    <div class="min-h-screen flex items-center justify-center p-4">
        <div class="w-full max-w-4xl bg-white rounded-lg shadow-sm overflow-hidden">
            <!-- Header -->
            <div class="bg-brand p-6 text-white">
                <div class="flex justify-between items-center mb-4">
                    <a href="<?= $this->url('frontend') ?>" class="flex items-center gap-2 text-white/80 hover:text-white transition-colors">
                        <i class="ph ph-arrow-left"></i>
                        <span>Back</span>
                    </a>
                    <h1 class="text-2xl font-light"><?= $this->t('Create Account') ?></h1>
                </div>
                <p class="text-white/90 text-sm"><?= sprintf($this->t('Join %s today'), $this->option('service.name.full')) ?></p>
            </div>

            <!-- Content -->
            <div class="p-6 md:p-8">
                <?php if ($this->option('service.user.registration') == 'false'): ?>
                    <!-- Registration disabled message -->
                    <div class="bg-amber-50 border border-amber-200 rounded-lg p-4 mb-6 flex items-start gap-3">
                        <i class="ph ph-warning text-amber-500 mt-0.5"></i>
                        <div class="text-amber-800 text-sm">
                            <?php if ($this->option('service.user.registration.message')): ?>
                                <?= $this->option('service.user.registration.message') ?>
                            <?php else: ?>
                                <?= $this->t('We are very sorry, but the registration is currently not possible.') ?>
                            <?php endif; ?>
                        </div>
                    </div>
                <?php else: ?>
                    <!-- Registration Form -->
                    <?php
                    $form = $this->registrationForm;
                    $form->setAttribute('method', 'post');
                    $form->setAttribute('action', $this->url('user/registration'));
                    $form->prepare();
                    ?>
                    <?= $this->form()->openTag($form) ?>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                        <!-- Left Column: Account Details -->
                        <div>
                            <div class="flex items-center gap-3 mb-6">
                                <div class="w-8 h-8 rounded-full bg-brand flex items-center justify-center text-white font-medium">1</div>
                                <h2 class="text-lg font-medium"><?= $this->t('Account Details') ?></h2>
                            </div>

                            <div class="space-y-5">
                                <!-- Email fields -->
                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1"><?= $this->t('Email Address') ?></label>
                                    <div class="relative">
                                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none text-gray-400">
                                            <i class="ph ph-envelope"></i>
                                        </div>
                                        <?= $this->formElement(
                                            $form->get('rf-email1')
                                                ->setAttribute('type', 'email')
                                                ->setAttribute('autocomplete', 'email')
                                                ->setAttribute('required', 'required')
                                                ->setAttribute('placeholder', 'your@email.com')
                                                ->setAttribute('class', 'w-full pl-10 pr-3 py-2.5 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand outline-none transition')
                                        ) ?>
                                    </div>
                                    <?php if ($form->get('rf-email1')->getMessages()): ?>
                                        <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                                            <i class="ph ph-warning text-xs"></i>
                                            <span><?= implode(', ', $form->get('rf-email1')->getMessages()) ?></span>
                                        </div>
                                    <?php endif; ?>
                                </div>

                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1"><?= $this->t('Confirm Email') ?></label>
                                    <div class="relative">
                                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none text-gray-400">
                                            <i class="ph ph-envelope"></i>
                                        </div>
                                        <?= $this->formElement(
                                            $form->get('rf-email2')
                                                ->setAttribute('type', 'email')
                                                ->setAttribute('autocomplete', 'email')
                                                ->setAttribute('required', 'required')
                                                ->setAttribute('placeholder', 'Confirm email')
                                                ->setAttribute('class', 'w-full pl-10 pr-3 py-2.5 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand outline-none transition')
                                        ) ?>
                                    </div>
                                    <?php if ($form->get('rf-email2')->getMessages()): ?>
                                        <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                                            <i class="ph ph-warning text-xs"></i>
                                            <span><?= implode(', ', $form->get('rf-email2')->getMessages()) ?></span>
                                        </div>
                                    <?php endif; ?>
                                </div>

                                <!-- Password fields -->
                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1"><?= $this->t('Password') ?></label>
                                    <div class="relative">
                                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none text-gray-400">
                                            <i class="ph ph-lock"></i>
                                        </div>
                                        <?= $this->formElement(
                                            $form->get('rf-pw1')
                                                ->setAttribute('type', 'password')
                                                ->setAttribute('autocomplete', 'new-password')
                                                ->setAttribute('required', 'required')
                                                ->setAttribute('placeholder', 'Create password')
                                                ->setAttribute('class', 'w-full pl-10 pr-10 py-2.5 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand outline-none transition')
                                        ) ?>
                                        <button type="button" class="absolute inset-y-0 right-0 pr-3 flex items-center text-gray-400 hover:text-gray-600">
                                            <i class="ph ph-eye-slash password-toggle" data-target="rf-pw1"></i>
                                        </button>
                                    </div>
                                    <?php if ($form->get('rf-pw1')->getMessages()): ?>
                                        <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                                            <i class="ph ph-warning text-xs"></i>
                                            <span><?= implode(', ', $form->get('rf-pw1')->getMessages()) ?></span>
                                        </div>
                                    <?php endif; ?>
                                </div>

                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1"><?= $this->t('Confirm Password') ?></label>
                                    <div class="relative">
                                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none text-gray-400">
                                            <i class="ph ph-lock"></i>
                                        </div>
                                        <?= $this->formElement(
                                            $form->get('rf-pw2')
                                                ->setAttribute('type', 'password')
                                                ->setAttribute('autocomplete', 'new-password')
                                                ->setAttribute('required', 'required')
                                                ->setAttribute('placeholder', 'Confirm password')
                                                ->setAttribute('class', 'w-full pl-10 pr-10 py-2.5 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand outline-none transition')
                                        ) ?>
                                        <button type="button" class="absolute inset-y-0 right-0 pr-3 flex items-center text-gray-400 hover:text-gray-600">
                                            <i class="ph ph-eye-slash password-toggle" data-target="rf-pw2"></i>
                                        </button>
                                    </div>
                                    <?php if ($form->get('rf-pw2')->getMessages()): ?>
                                        <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                                            <i class="ph ph-warning text-xs"></i>
                                            <span><?= implode(', ', $form->get('rf-pw2')->getMessages()) ?></span>
                                        </div>
                                    <?php endif; ?>
                                </div>
                            </div>
                        </div>

                        <!-- Right Column: Personal Information -->
                        <div>
                            <div class="flex items-center gap-3 mb-6">
                                <div class="w-8 h-8 rounded-full bg-brand flex items-center justify-center text-white font-medium">2</div>
                                <h2 class="text-lg font-medium"><?= $this->t('Personal Information') ?></h2>
                            </div>

                            <!-- Gender -->
                            <div class="space-y-5">
                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1"><?= $this->t('Gender') ?></label>
                                    <?= $this->formElement(
                                        $form->get('rf-gender')
                                            ->setAttribute('class', 'w-full py-2.5 px-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand outline-none transition')
                                    ) ?>
                                    <?php if ($form->get('rf-gender')->getMessages()): ?>
                                        <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                                            <i class="ph ph-warning text-xs"></i>
                                            <span><?= implode(', ', $form->get('rf-gender')->getMessages()) ?></span>
                                        </div>
                                    <?php endif; ?>
                                </div>
                            </div>


                            <!-- Name fields -->
                            <div class="grid grid-cols-2 gap-4">
                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1"><?= $this->t('First Name') ?></label>
                                    <?= $this->formElement(
                                        $form->get('rf-firstname')
                                            ->setAttribute('type', 'text')
                                            ->setAttribute('autocomplete', 'given-name')
                                            ->setAttribute('required', 'required')
                                            ->setAttribute('placeholder', 'John')
                                            ->setAttribute('class', 'w-full py-2.5 px-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand outline-none transition')
                                    ) ?>
                                    <?php if ($form->get('rf-firstname')->getMessages()): ?>
                                        <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                                            <i class="ph ph-warning text-xs"></i>
                                            <span><?= implode(', ', $form->get('rf-firstname')->getMessages()) ?></span>
                                        </div>
                                    <?php endif; ?>
                                </div>

                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1"><?= $this->t('Last Name') ?></label>
                                    <?= $this->formElement(
                                        $form->get('rf-lastname')
                                            ->setAttribute('type', 'text')
                                            ->setAttribute('autocomplete', 'family-name')
                                            ->setAttribute('required', 'required')
                                            ->setAttribute('placeholder', 'Doe')
                                            ->setAttribute('class', 'w-full py-2.5 px-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand outline-none transition')
                                    ) ?>
                                    <?php if ($form->get('rf-lastname')->getMessages()): ?>
                                        <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                                            <i class="ph ph-warning text-xs"></i>
                                            <span><?= implode(', ', $form->get('rf-lastname')->getMessages()) ?></span>
                                        </div>
                                    <?php endif; ?>
                                </div>
                            </div>

                            <!-- Address -->
                            <div class="grid grid-cols-1 gap-4">
                                <div class="col-span-2">
                                    <label class="block text-sm font-medium text-gray-700 mb-1"><?= $this->t('Street Address') ?></label>
                                    <?= $this->formElement(
                                        $form->get('rf-street')
                                            ->setAttribute('type', 'text')
                                            ->setAttribute('autocomplete', 'street-address')
                                            ->setAttribute('required', 'required')
                                            ->setAttribute('placeholder', 'Street name')
                                            ->setAttribute('class', 'w-full py-2.5 px-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand outline-none transition')
                                    ) ?>
                                    <?php if ($form->get('rf-street')->getMessages()): ?>
                                        <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                                            <i class="ph ph-warning text-xs"></i>
                                            <span><?= implode(', ', $form->get('rf-street')->getMessages()) ?></span>
                                        </div>
                                    <?php endif; ?>
                                </div>

                                <!-- <div>
                                        <label class="block text-sm font-medium text-gray-700 mb-1"><?= $this->t('Number') ?></label>
                                        <?= $this->formElement(
                                            $form->get('rf-number')
                                                ->setAttribute('type', 'text')
                                                ->setAttribute('required', 'required')
                                                ->setAttribute('placeholder', '123')
                                                ->setAttribute('class', 'w-full py-2.5 px-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand outline-none transition')
                                        ) ?>
                                        <?php if ($form->get('rf-number')->getMessages()): ?>
                                            <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                                                <i class="ph ph-warning text-xs"></i>
                                                <span><?= implode(', ', $form->get('rf-number')->getMessages()) ?></span>
                                            </div>
                                        <?php endif; ?>
                                    </div> -->
                            </div>

                            <!-- City and ZIP -->
                            <div class="grid grid-cols-2 gap-4">
                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1"><?= $this->t('ZIP Code') ?></label>
                                    <?= $this->formElement(
                                        $form->get('rf-zip')
                                            ->setAttribute('type', 'text')
                                            ->setAttribute('autocomplete', 'postal-code')
                                            ->setAttribute('required', 'required')
                                            ->setAttribute('placeholder', '12345')
                                            ->setAttribute('class', 'w-full py-2.5 px-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand outline-none transition')
                                    ) ?>
                                    <?php if ($form->get('rf-zip')->getMessages()): ?>
                                        <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                                            <i class="ph ph-warning text-xs"></i>
                                            <span><?= implode(', ', $form->get('rf-zip')->getMessages()) ?></span>
                                        </div>
                                    <?php endif; ?>
                                </div>

                                <div>
                                    <label class="block text-sm font-medium text-gray-700 mb-1"><?= $this->t('City') ?></label>
                                    <?= $this->formElement(
                                        $form->get('rf-city')
                                            ->setAttribute('type', 'text')
                                            ->setAttribute('autocomplete', 'address-level2')
                                            ->setAttribute('required', 'required')
                                            ->setAttribute('placeholder', 'Your city')
                                            ->setAttribute('class', 'w-full py-2.5 px-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand outline-none transition')
                                    ) ?>
                                    <?php if ($form->get('rf-city')->getMessages()): ?>
                                        <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                                            <i class="ph ph-warning text-xs"></i>
                                            <span><?= implode(', ', $form->get('rf-city')->getMessages()) ?></span>
                                        </div>
                                    <?php endif; ?>
                                </div>
                            </div>

                            <!-- Phone -->
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1"><?= $this->t('Phone Number') ?></label>
                                <div class="relative">
                                    <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none text-gray-400">
                                        <i class="ph ph-phone"></i>
                                    </div>
                                    <?= $this->formElement(
                                        $form->get('rf-phone')
                                            ->setAttribute('type', 'tel')
                                            ->setAttribute('autocomplete', 'tel')
                                            ->setAttribute('placeholder', '+1 (555) 123-4567')
                                            ->setAttribute('class', 'w-full pl-10 pr-3 py-2.5 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand outline-none transition')
                                    ) ?>
                                </div>
                                <?php if ($form->get('rf-phone')->getMessages()): ?>
                                    <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                                        <i class="ph ph-warning text-xs"></i>
                                        <span><?= implode(', ', $form->get('rf-phone')->getMessages()) ?></span>
                                    </div>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
            </div>

            <!-- Hidden fields -->
            <div class="hidden">
                <?= $this->formElement($form->get('rf-nickname')) ?>
                <?= $this->formElement($form->get('rf-csrf')) ?>
            </div>

            <!-- Terms and Privacy -->
            <div class="mt-8 pt-6 border-t border-gray-200">
                <h3 class="text-lg font-medium mb-4"><?= $this->t('Terms & Conditions') ?></h3>
                <?php
                    $html = '';
                    foreach (['terms', 'privacy'] as $option) {
                        $optionFile = $this->option('service.user.registration.' . $option . '.file');
                        if ($optionFile && $form->has('rf-' . $option)) {
                            $optionName = $this->option('service.user.registration.' . $option . '.name', basename($optionFile));
                            $html .= sprintf(
                                '<div class="flex items-start gap-3 mb-3">
                                         %s
                                         <label class="text-sm text-gray-600 cursor-pointer">
                                             %s <a href="%s" target="_blank" class="text-brand hover:text-brand-dark font-medium transition-colors">%s</a>
                                         </label>
                                     </div>',
                                $this->formElement($form->get('rf-' . $option)->setAttribute('class', 'mt-1 h-4 w-4 text-brand border-gray-300 rounded focus:ring-brand')),
                                sprintf($this->formLabel($form->get('rf-' . $option)), ''),
                                $this->basePath($optionFile),
                                $optionName
                            );
                        }
                    }
                    if (!$this->option('service.user.registration.privacy.file')) {
                        if ($this->option('client.website.privacy')) {
                            $html .= sprintf(
                                '<div class="flex items-start gap-3 mb-3">
                                        <input type="checkbox" required class="mt-1 h-4 w-4 text-brand border-gray-300 rounded focus:ring-brand">
                                        <label class="text-sm text-gray-600 cursor-pointer">
                                            %s <a href="%s" target="_blank" class="text-brand hover:text-brand-dark font-medium transition-colors">%s</a>
                                        </label>
                                    </div>',
                                sprintf($this->t('I have read and accept the %1$sprivacy policy%2$s'), '', ''),
                                $this->option('client.website.privacy'),
                                $this->t('privacy policy')
                            );
                        }
                    }
                    if ($html) {
                        echo $html;
                    }
                ?>
                <?php if ($form->has('rf-terms') && $form->get('rf-terms')->getMessages()): ?>
                    <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                        <i class="ph ph-warning text-xs"></i>
                        <span><?= implode(', ', $form->get('rf-terms')->getMessages()) ?></span>
                    </div>
                <?php endif; ?>
                <?php if ($form->has('rf-privacy') && $form->get('rf-privacy')->getMessages()): ?>
                    <div class="mt-1 text-sm text-red-600 flex items-center gap-1">
                        <i class="ph ph-warning text-xs"></i>
                        <span><?= implode(', ', $form->get('rf-privacy')->getMessages()) ?></span>
                    </div>
                <?php endif; ?>
            </div>

            <!-- Submit button -->
            <div class="mt-8">
                <?= $this->formElement(
                        $form->get('rf-submit')
                            ->setAttribute('type', 'submit')
                            ->setAttribute('class', 'w-full bg-brand hover:bg-brand-dark text-white font-medium py-3 px-4 rounded-md transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-brand')
                    ) ?>
            </div>

            <?= $this->form()->closeTag() ?>

            <!-- Login link -->
            <div class="mt-6 text-center">
                <p class="text-gray-600">
                    Already have an account?
                    <a href="<?= $this->url('user/login') ?>" class="text-brand hover:text-brand-dark font-medium inline-flex items-center gap-1 transition-colors">
                        Sign in here <i class="ph ph-arrow-right"></i>
                    </a>
                </p>
            </div>
        <?php endif; ?>
        </div>
    </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Password toggle functionality
            const passwordToggles = document.querySelectorAll('.password-toggle');
            passwordToggles.forEach(toggle => {
                toggle.addEventListener('click', function() {
                    const targetName = this.getAttribute('data-target');
                    const passwordInput = document.querySelector(`[name="${targetName}"]`);
                    if (passwordInput) {
                        const isPassword = passwordInput.getAttribute('type') === 'password';
                        passwordInput.setAttribute('type', isPassword ? 'text' : 'password');
                        this.classList.toggle('ph-eye');
                        this.classList.toggle('ph-eye-slash');
                    }
                });
            });

            // Form validation feedback
            const inputs = document.querySelectorAll('input, select');
            inputs.forEach(input => {
                input.addEventListener('blur', function() {
                    if (this.value && !this.checkValidity()) {
                        this.classList.add('border-red-500');
                        this.classList.remove('border-gray-300');
                    } else if (this.value) {
                        this.classList.remove('border-red-500');
                        this.classList.add('border-green-500');
                    }
                });

                input.addEventListener('input', function() {
                    if (this.classList.contains('border-red-500') || this.classList.contains('border-green-500')) {
                        if (this.checkValidity()) {
                            this.classList.remove('border-red-500');
                            this.classList.add('border-green-500');
                        } else {
                            this.classList.remove('border-green-500');
                            this.classList.add('border-red-500');
                        }
                    }
                });
            });

            // Email confirmation validation
            const emailInputs = document.querySelectorAll('input[type="email"]');
            if (emailInputs.length >= 2) {
                function validateEmailMatch() {
                    if (emailInputs[1].value && emailInputs[0].value !== emailInputs[1].value) {
                        emailInputs[1].classList.add('border-red-500');
                        emailInputs[1].classList.remove('border-gray-300', 'border-green-500');
                    } else if (emailInputs[1].value && emailInputs[0].value === emailInputs[1].value) {
                        emailInputs[1].classList.remove('border-red-500');
                        emailInputs[1].classList.add('border-green-500');
                    }
                }
                emailInputs[0].addEventListener('input', validateEmailMatch);
                emailInputs[1].addEventListener('input', validateEmailMatch);
            }

            // Password confirmation validation
            const passwordInputs = document.querySelectorAll('input[type="password"]');
            if (passwordInputs.length >= 2) {
                function validatePasswordMatch() {
                    if (passwordInputs[1].value && passwordInputs[0].value !== passwordInputs[1].value) {
                        passwordInputs[1].classList.add('border-red-500');
                        passwordInputs[1].classList.remove('border-gray-300', 'border-green-500');
                    } else if (passwordInputs[1].value && passwordInputs[0].value === passwordInputs[1].value) {
                        passwordInputs[1].classList.remove('border-red-500');
                        passwordInputs[1].classList.add('border-green-500');
                    }
                }
                passwordInputs[0].addEventListener('input', validatePasswordMatch);
                passwordInputs[1].addEventListener('input', validatePasswordMatch);
            }

            // Form submission enhancement
            const form = document.querySelector('form');
            const submitButton = document.querySelector('button[type="submit"]');
            if (form && submitButton) {
                form.addEventListener('submit', function(e) {
                    submitButton.disabled = true;
                    submitButton.innerHTML = '<i class="ph ph-circle-notch animate-spin"></i> Creating Account...';

                    // Re-enable button after 5 seconds as fallback
                    setTimeout(() => {
                        if (submitButton.disabled) {
                            submitButton.disabled = false;
                            submitButton.innerHTML = 'Create Account';
                        }
                    }, 5000);
                });
            }
        });
    </script>
</body>

</html>