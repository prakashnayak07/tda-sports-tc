<?php

$this->setup(array(
    'title' => 'Login',
    'panel' => 'centered-panel',
    'messages' => array(
        'error' => sprintf($this->loginMessage ?: '', $this->loginDetent ? $this->timeFormat($this->loginDetent) : ''),
    ),
    'back' => false,
    'links' => array(
        'Register now' => $this->url('user/registration'),
        'Forgot password?' => $this->url('user/password'),
    ),
));

?>

<!DOCTYPE html>
<html lang="<?= $this->config('i18n.locale') ?>">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= $this->option(['client.name.short', 'service.name.full']) ?> - Login</title>



    <!-- Custom styles -->
    <style>
        body {
            font-family: 'Inter', Arial, sans-serif;
            background: white;
            margin: 0;
            padding: 0;
        }

        /* Fix input field text visibility and borders */
        input[type="email"],
        input[type="password"],
        input[type="text"] {
            color: #374151 !important;
            background-color: white !important;
            border: 1px solid #d1d5db !important;
            border-radius: 0.375rem !important;
        }

        input[type="email"]:focus,
        input[type="password"]:focus,
        input[type="text"]:focus {
            border-color: #10b981 !important;
            outline: none !important;
            box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.1) !important;
        }

        input[type="email"]::placeholder,
        input[type="password"]::placeholder,
        input[type="text"]::placeholder {
            color: #9CA3AF !important;
        }

        /* Ensure form elements are properly styled */
        .form-input {
            color: #374151 !important;
            background-color: white !important;
            border: 1px solid #d1d5db !important;
        }

        /* Remove any conflicting styles */
        .default-table input,
        .default-form-label-row input {
            border: 1px solid #d1d5db !important;
            background-color: white !important;
        }
    </style>
</head>

<body>
    <section id="email-login-form" class="bg-white min-h-screen">
        <div class="flex min-h-screen flex-col justify-center px-4 py-12 sm:px-6 lg:px-8">
            <div class="mx-auto w-full max-w-md p-8 shadow-md rounded-lg">

                <!-- Page Header -->
                <div class="mb-8 text-center">
                    <h1 class="text-2xl font-bold text-gray-900 mb-2"><?= $this->t('Login') ?></h1>
                    <p class="text-sm text-gray-600">Welcome back! Please sign in to your account.</p>
                </div>

                <!-- Back to main link -->
                <div class="mb-6 text-center">
                    <a href="<?= $this->url('frontend') ?>" class="inline-flex items-center text-sm font-medium text-gray-600 hover:text-gray-800 transition-colors duration-200">
                        <i class="ph ph-arrow-left mr-2"></i>
                        Go back to main
                    </a>
                </div>

                <!-- Error messages -->
                <?php if (isset($this->messages['error']) && $this->messages['error']): ?>
                    <div class="mb-6 rounded-md bg-red-50 p-4 border border-red-200">
                        <div class="flex">
                            <div class="flex-shrink-0">
                                <i class="ph ph-warning text-red-400 text-lg"></i>
                            </div>
                            <div class="ml-3">
                                <p class="text-sm text-red-800 font-medium">
                                    <?= $this->messages['error'] ?>
                                </p>
                            </div>
                        </div>
                    </div>
                <?php endif; ?>

                <!-- Login description -->
                <div class="mb-6 text-center">
                    <p class="text-sm text-gray-600">
                        <?= sprintf(
                            $this->t('If you have already registered, you can login here with your email address and start booking %s.'),
                            $this->option('subject.square.type.plural')
                        ) ?>
                    </p>
                </div>

                <!-- Divider -->
                <div class="relative my-6 flex items-center justify-center">
                    <div class="flex-grow border-t border-gray-300"></div>
                    <span class="mx-4 flex-shrink text-sm text-gray-500 bg-white px-2">or Login with Email</span>
                    <div class="flex-grow border-t border-gray-300"></div>
                </div>

                <!-- Login Form -->
                <div class="mt-6">
                    <?php
                    $form = $this->loginForm;
                    $form->setAttribute('method', 'post');
                    $form->setAttribute('action', $this->url('user/login'));
                    $form->prepare();
                    ?>

                    <?= $this->form()->openTag($form) ?>

                    <!-- Email field -->
                    <div class="mb-6">
                        <label for="email" class="block text-sm font-medium text-gray-700 mb-2">Email</label>
                        <div class="relative">
                            <?= $this->formElement(
                                $form->get('lf-email')
                                    ->setAttribute('id', 'email')
                                    ->setAttribute('name', 'lf-email')
                                    ->setAttribute('type', 'email')
                                    ->setAttribute('autocomplete', 'email')
                                    ->setAttribute('required', 'required')
                                    ->setAttribute('placeholder', 'E.g. johndoe@email.com')
                                    ->setAttribute('class', 'form-input block w-full appearance-none rounded-md border border-gray-300 pl-10 pr-12 py-3 placeholder-gray-400 shadow-sm focus:border-green-500 focus:outline-none focus:ring-green-500 sm:text-sm')
                            ) ?>
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <i class="ph ph-envelope text-gray-400 text-lg"></i>
                            </div>
                            <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                <span class="text-gray-400 text-sm font-medium">@</span>
                            </div>

                        </div>
                        <?php if ($form->get('lf-email')->getMessages()): ?>
                            <p class="mt-2 my-4 justify-center text-sm text-red-600 flex items-center">
                                <i class="ph ph-warning mr-1"></i>
                                <?= implode(', ', $form->get('lf-email')->getMessages()) ?>
                            </p>
                        <?php endif; ?>
                    </div>

                    <!-- Password field -->
                    <div class="mb-6">
                        <label for="password" class="block text-sm font-medium text-gray-700 mb-2">Password</label>
                        <div class="relative">
                            <?= $this->formElement(
                                $form->get('lf-pw')
                                    ->setAttribute('id', 'password')
                                    ->setAttribute('name', 'lf-pw')
                                    ->setAttribute('type', 'password')
                                    ->setAttribute('autocomplete', 'current-password')
                                    ->setAttribute('required', 'required')
                                    ->setAttribute('placeholder', 'Enter your password')
                                    ->setAttribute('class', 'form-input block w-full appearance-none rounded-md border border-gray-300 pl-10 pr-12 py-3 placeholder-gray-400 shadow-sm focus:border-green-500 focus:outline-none focus:ring-green-500 sm:text-sm')
                            ) ?>
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <i class="ph ph-lock text-gray-400 text-lg"></i>
                            </div>
                            <div class="absolute inset-y-0 right-0 flex cursor-pointer items-center pr-3">
                                <i class="ph ph-eye-slash text-gray-400 hover:text-gray-600 transition-colors duration-200 text-lg" id="password-toggle"></i>
                            </div>
                            <?php if ($form->get('lf-pw')->getMessages()): ?>
                                <p class="mt-2 text-sm text-red-600 flex items-center">
                                    <i class="ph ph-warning mr-1"></i>
                                    <?= implode(', ', $form->get('lf-pw')->getMessages()) ?>
                                </p>
                            <?php endif; ?>
                        </div>
                    </div>

                    <!-- Remember me and Forgot password -->
                    <div class="flex items-center justify-between mb-6">
                        <div class="flex items-center">
                            <input id="remember-me" name="remember-me" type="checkbox" class="h-4 w-4 rounded border-gray-300 text-green-600 focus:ring-green-500" />
                            <label for="remember-me" class="ml-2 block text-sm text-gray-900">Remember Me</label>
                        </div>
                        <div class="text-sm">
                            <a href="<?= $this->url('user/password') ?>" class="font-medium text-green-600 hover:text-green-500 transition-colors duration-200">
                                Forgot Password?
                            </a>
                        </div>
                    </div>

                    <!-- Submit button -->
                    <div>
                        <?= $this->formElement(
                            $form->get('lf-submit')
                                ->setAttribute('type', 'submit')
                                ->setAttribute('class', 'flex w-full justify-center rounded-md border border-transparent bg-green-600 px-4 py-3 text-sm font-semibold text-white shadow-sm hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 transition-colors duration-200')
                        ) ?>
                    </div>

                    <?= $this->form()->closeTag() ?>

                    <!-- Register link -->
                    <div class="mt-8 text-center">
                        <p class="text-sm text-gray-600">
                            Not registered yet?
                            <a href="<?= $this->url('user/registration') ?>" class="inline-flex items-center font-medium text-green-600 hover:text-green-500 transition-colors duration-200">
                                Create an account
                                <i class="ph ph-arrow-up-right ml-1"></i>
                            </a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script>
        // Password toggle functionality
        document.addEventListener('DOMContentLoaded', function() {
            const passwordToggle = document.getElementById('password-toggle');
            const passwordInput = document.getElementById('password');

            if (passwordToggle && passwordInput) {
                passwordToggle.addEventListener('click', function() {
                    const type = passwordInput.getAttribute('type') === 'password' ? 'text' : 'password';
                    passwordInput.setAttribute('type', type);

                    // Toggle icon
                    if (type === 'text') {
                        passwordToggle.className = 'ph ph-eye text-gray-400 hover:text-gray-600 transition-colors duration-200 text-lg';
                    } else {
                        passwordToggle.className = 'ph ph-eye-slash text-gray-400 hover:text-gray-600 transition-colors duration-200 text-lg';
                    }
                });
            }

            // Add focus effects for better UX
            const inputs = document.querySelectorAll('input[type="email"], input[type="password"]');
            inputs.forEach(input => {
                input.addEventListener('focus', function() {
                    this.parentElement.classList.add('ring-2', 'ring-green-500', 'ring-opacity-50');
                });

                input.addEventListener('blur', function() {
                    this.parentElement.classList.remove('ring-2', 'ring-green-500', 'ring-opacity-50');
                });
            });
        });
    </script>
</body>

</html>